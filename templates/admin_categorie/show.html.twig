{% extends 'admin/base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex">
        <a class="px-1" href="{{ path('app_admin_categorie_index') }}"><button class="btn btn-primary">back to list</button></a>
    
        <a class="px-1" href="{{ path('app_admin_categorie_edit', {'id': categorie.id}) }}"><button class="btn btn-success">edit</button></a>
    {{ include('admin_categorie/_delete_form.html.twig') }}
        
    </div>

{% endblock %}
