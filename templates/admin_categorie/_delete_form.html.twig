<form class="px-1" method="post" action="{{ path('app_admin_categorie_delete', {'id': categorie.id}) }}"     >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
